{
  "name": "@cadaster/adapter-mock",
  "version": "0.0.0",
  "private": true,
  "description": "GeoJSON Adapter w/ full-text search",
  "author": "Miro <miro.ter.seri@gmail.com>",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "test": "ava --verbose --color",
    "test:coverage": "nyc --reporter=text ava --verbose --color",
    "test:watch": "ava -vw"
  },
  "devDependencies": {
    "@cadaster/mock-features-distinct-places": "*",
    "ava": "^2.0.0",
    "nyc": "^14.1.1"
  },
  "dependencies": {
    "@cadaster/types": "*",
    "fuse.js": "^3.4.5",
    "ramda": "^0.26.1"
  }
}
